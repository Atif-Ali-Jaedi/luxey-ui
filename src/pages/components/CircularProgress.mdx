import { circularProgressCodes } from "@/data/component-codes/circular-progress";

<BCrumbs data={["Docs", "Components", "Circular Progress"]} />

<Article>

# Circular Progress

Circular progress indicators are utilized to indicate an undetermined wait period or visually represent the duration of a process.

## Slots 

- **circular-progress**: The base slot of the circular progress, it is the main container.
- **svg-wrapper**: The wrapper of the svg circles and the value label.
- **progress**: The svg element of the circles.
- **track**: The track is the background circle of the circular progress.
- **indicator**: The indicator is the one that is filled according to the value.
- **value**: The value content.
- **progress-label**: The label content.

## Usage

<CodePreview code={circularProgressCodes.usage} />

> **Note**: You can disable the progress using the `data-disabled` attribute and can also disable the animation using the `data-disable-animation` attribute.

## Sizes

<CodePreview code={circularProgressCodes.sizes} />

## Colors

<CodePreview code={circularProgressCodes.colors} />

## With Label

<CodePreview code={circularProgressCodes.withLabel} />

## With Value

<CodePreview code={circularProgressCodes.withValue} />

<CompsLinks 
  last={{link:"../chip", text: "Chip"}}
  next={{link:"../code", text: "Code"}}
/>

</Article>