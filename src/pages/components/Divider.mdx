import { dividerCodes } from "@/data/component-codes/divider";

<BCrumbs data={["Docs", "Components", "Divider"]} />

<Article>

# Divider

A separator for contents.

## Usage

<CodePreview code={dividerCodes.default} />

> **Note**: You must set a height to the parent or to the divider itself when the attribute `data-orientation` is set to `vertical`.

<CompsLinks
  last={{ link: "../code", text: "Code" }}
  next={{ link: "../image", text: "Image" }}
/>

</Article>