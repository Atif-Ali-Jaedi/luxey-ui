import { currentVersion } from "@/data/js/constants";
import { Timeline } from "@/components"
import { Helmet } from "react-helmet-async";

<Helmet>
  <title>Installation | LuxeyUI - Elevate your design effortlessly and quickly</title> 
  <meta name="description" content="The easiest way to get started with LuxeyUI is to install it globally. You can either use CDN or just install it via a package manager in your project. So you can get all the components at once."/>
</Helmet>
<BCrumbs data={["Docs", "Installation"]} />
<Article>

# Installation

To install you can either use <span className="inline-flex gap-2 items-center translate-y-1.5"><img src="https://cdn.jsdelivr.net/gh/devicons/devicon@latest/icons/vitejs/vitejs-original.svg" className="w-6 !my-0" /><ELink text="Vite" href="https://vitejs.dev"/></span> or just plain HTML.

## Vite

<Timeline
  data={[
    {
      id: 1,
      heading: "Create a project",
      html: `Start by creating a new project using <code class="code">Vite</code>:`,
      code: {
        lang: "bash",
        code: `npm create vite@latest ./`
      }
    },
    {
     id: 2,
     heading: "Install TailwindCSS and LuxeyUI",
     html: `Install <code class="code">tailwindcss</code> and its peer dependencies, then generate your <code class="code">tailwind.config.js</code> and <code class="code">postcss.config.js</code> files and configure them as needed along with the library itself:`,
     code: {
      lang: "bash",
      code: `npm i -D tailwindcss postcss autoprefixer luxeyui@latest
npx tailwindcss init -p`
     }
    },
    {
      id: 3,
      heading: "Configure tailwind",
      html: `Add the following code to your <code class="code">tailwind.config.js</code> file if you want to use dark mode:`,
      code: {
        lang: "js",
        code: `/** @type {import('tailwindcss').Config} */
module.exports = {
  darkMode: "class",
  content: [
    "./index.html",
    "./node_modules/luxeyui/dist/all.min.js"
  ]
}`
      }
    },
    {
      id: 4,
      heading: "That's it",
      text: "Now you can start your designing using components with full functionality.",
      code: { lang: "html", code: `<button class="button primary">Button</button>` }
    }
  ]}
/>

---

## HTML 

<Timeline
  data={[
    {
      id: 1,
      heading: "Create a boilerplate",
      html: `Start by creating a basic HTMl template in your <code class="code">index.html</code> file. You can copy & paste the code given below.`,
      code: {
        lang: "html",
        code: `<!DOCTYPE html>
<html lang="en">
	<head>
	<meta charset="UTF-8" />
  	<meta name="viewport" content="width=device-width, initial-scale=1.0" />
  	<meta http-equiv="X-UA-Compatible" content="ie=edge" />
  	<title>LuxeyUI Project</title>
	</head>
	<body></body>
</html>`
      }
    },
    {
      id: 2,
      heading: "Add the CDN",
      text: "Add the stylesheet and script to your HTML file. You can add TailwindCSS for better customization.",
      code: {
        lang: "html",
        code: `<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/luxeyui@${currentVersion}/dist/all.min.css" />
<script src="https://cdn.tailwindcss.com"></script>
<script src="https://cdn.jsdelivr.net/npm/luxeyui@${currentVersion}/dist/all.min.js" defer></script>`
      }
    },
    {
      id: 3,
      heading: "Configure tailwind",
      text: `Add the following code to your script if you want to use dark mode:`,
      code: {
        lang: "html",
        code: `<script>
  tailwind.config = {
    darkMode: "class",
    content: ["./index.html"]
  }
</script>`
      }
    },
    {
      id: 4,
      heading: "That's it",
      text: "Now you can start your designing using components with full functionality.",
      code: { lang: "html", code: `<button class="button primary">Button</button>` }
    }
  ]}
/>

</Article>

